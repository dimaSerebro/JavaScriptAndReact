"use strict";

const arr = [10, 23, 25, 17, 0, 43, 38, 84];
arr.sort();
console.log(arr);

function compareNum (a, b) {
   return a - b;
}
// arr[99] = 0;
// console.log(arr.length);
// console.log(arr);

/* arr.forEach (function (item, i, arr) {
   console.log(`${i}: ${item} внутри массива ${arr}`);
}); */

//arr.pop();
// arr.push(11);

// console.log(arr);

// Перебор массива
/* for (let i = 0; i < arr.length; i++) {
   console.log(arr[i]);
} */

// Перебор массива
/* for (let value of arr) {
   console.log(value);
} */


/* const str = prompt("", "");
const products = str.split(", ");
products.sort();
console.log(products.join("; ")); */


